<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.Schulprojekt.helloprojekt.ContactSearchActivity" >

    <Button
        android:id="@+id/btnContactSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/txtContactSearch"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/txtContactSearch"
        android:layout_marginBottom="60dp"
        android:text="Suchen" />

    <EditText
        android:id="@+id/txtContactSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnContactSearch"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="32dp"
        android:ems="10" />

    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/txtContactSearch"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="66dp"
        android:src="@drawable/smile" />

</RelativeLayout>
